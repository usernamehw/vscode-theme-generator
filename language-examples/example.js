// Comment
/**
 * Function description
 * @param {string} param1 description1
 * @param {number} param2 description2
 */
function functionn(param1, param2) {
	/^[^A-Za-z0-9]hey|ho(\d)*\d{3,5}(?<year>\d{4})$/.test('a');
	for (var i = 0; i < param1.length; i++) {
		text += i;
	}
	return `${param1} = ${fun[0]} \n\n \t ${[param1].toString()}`;
}
console.warn(null, true, false, undefined, NaN, Infinity, -Infinity, 10);
Object; Function; Boolean; Symbol; Error; Number; Date; String; RegExp; Array; Map; Set; ArrayBuffer; Reflect; Intl;

new Map();
JSON;
Math.PI;
arguments;
decodeURI;
parseInt
String(12);
new Array();

window.document.addEventListener('click', (e) => console.warn(1));

String.prototype.something = () => {
	return this.reverse();
}

function fun() { }
var fun = () => 0;
let func = function () { }

class Cla {
	meth() { }
	meth = () => 0;
	meth = function () { }
}